### Monolith 프로젝트와 MSA 프로젝트
- Monolith(모노리스) 프로젝트: 하나의 프로그램(서버) 안에 화면, 비즈니스 로직, 데이터베이스 접근 등 모든 기능이 통째로 들어있는 구조

  - 작동 방식: 내부 자바 메서드 호출이나 직접적인 단일 DB 조회로 데이터를 처리함

  - 예시 스택: Spring Boot + MyBatis (SQL Mapper) + Thymeleaf (Template 엔진)

    ##### ex) 초기 스타트업, 스택 오버플로우(Stack Overflow)

- MSA(마이크로 서비스) 프로젝트: 하나의 거대한 서비스를 기능별(회원, 주문, 결제 등)로 잘게 쪼개어, 독립된 여러 개의 작은 서버로 운영하는 구조

  - 작동 방식: 분리된 서버들끼리 필요한 데이터를 주고받기 위해 API(HTTP/REST, gRPC) 통신을 주고받으며 협업함

  - 예시 스택: React/Next.js (프론트 서버) + Spring Boot/JPA (회원 API 서버) + Node.js (결제 API 서버)

    ##### ex) 넷플릭스, 쿠팡, 배달의민족
    
--------

### MyBatis와 JdbcTemplate

#### MyBatis 
  - XML 사용
    ```xml
    <mapper namespace="com.board.mapper.BoardMapper">

    <!-- 게시물 목록 보기 -->
    <select id="list" parameterType="HashMap" resultType="com.board.dto.BoardDTO">
      select seq,seqno,title,writer,regdate,hitno from (select row_number() over (order by seqno desc)
          as seq,seqno, title, writer, hitno,regdate from tbl_board
          where title like '%'||#{keyword}||'%' or writer like '%'||#{keyword}||'%' or content like '%'||#{keyword}||'%')
   	  where seq between #{startPoint} and #{endPoint}
    </select>
    ```
  - @Mapper 인터페이스 (정교한 SQL 생성 어려움 -> 대형 프로젝트에서는 사용x)
    * 스프링 레거시 프로젝트에서는 @Mapper 인터페이스 사용 시 root.context.xml에 아래 코드 추가

      ```xml
      <mybatis:scan base-package="mapper 패키지 명" />
      ```
      
    ```java
    @Mapper
    public interface UserMapper {

    @Select("select id, name, email from users")
    List<User> findAll();

    @Select("select id, name, email from users where id = #{id}")
    User findById(Long id);

    @Insert("insert into users(name, email) values(#{name}, #{email})")
    int insert(User user);
    //int는 성공  여부에 따라 row 값 반환
    }
    ```
#### JdbcTemplate (스프링 자체 객체)
  - 생성자 주입 방식으로 의존성 주입 -> 스프링빈으로 생성
    ```java
    public class UserRepository {
      private UserRepository(JdbcTemplate jdbcTemplate){
        this.jdbcTemplate = jdbcTemplate;
      }
    }
    ```

  - 데이터베이스 결과를 자바 객체로 변환해주는 클래스 생성
    ```java
    private final RowMapper<User> userRowMapper = (rs, rowNum) -> {
      User user = new User();
      user.setId(rs.getLong("id"));
      user.setName(rs.getString("name"));
      user.setEmail(rs.getString("email));
      return user;
    }

    public List<User> findAll() {
      String sql = "select id, name, email from users";
      return jdbcTemplate.query(sql, userRowMapper);
    }

    public User findById(Long Id) {
      String sql = "select id, name, email from users where id = ?"
      return jdbcTemplate.queryForObject(sql, userRowMapper, id);
    }

    public int save(User user) {
      String sql = "insert into users(name, email) values (?,?)";
      return jdbcTemplate.update(sql, user.getName(), user.getEmail());
    }
    ```
    
--------

### Oracle 의 rownum
  - 쿼리 결과로 나오는 각각의 행(Row)에 부여되는 가상의 순번
    * 작동 방식 : 행을 하나씩 읽어와서 where 조건을 만족하는 순간에 rownum을 1번부터 번호를 부여(행이 하나씩 생성될때마다 rownum 증가)

--------

### 배포 전 확인 사항

- Spring Boot 의 application.properties 의 민감한 내용들은 환경변수 등록 (ex: DBMS 설정 등) 
- React + Next.js 는 .env 파일을 .gitignore 파일에 등록 및 .env 파일 내에 환경 변수 등록
  - .env.development / .env.production 으로 설정 파일 분리
  - 환경 변수 이름은 NEXT_PUBLIC_ 접두사 사용

--------

### Spring Boot 설치 시 JWT(JSON WEB TOKEN) 설정
 - mvn 라이브러리에서 jjwt 검색 후 설치 
  - jjwt-api : 토큰을 다루는 자바 인터페이스와 기본 기능 제공
  - jjwt-impl : 암호와 알고리즘 등 실제 토큰을 생성하고 해석하는 구현체
  - jjwt-jackson : JSON 과 Java 객체 간의 변환 담당

--------

### 쿠키를 이용한 아이디/패스워드 저장 및 자동로그인 코드

```javascript
  // Cookie 가져 오기
  //next js의 cookies-next 라이브러리 에 있는 getCookie() 함수를 사용
  const emailCookie = getCookie('email') as string | undefined;
  const passwordCookie = getCookie('password') as string | undefined;
  const authkeyCookie = getCookie('authkey') as string | undefined; 

  const checkBoxConfirm = async (): Promise<void> => {
  
          // email 쿠키 존재 여부 확인 후 email 쿠키가 존재하면 email state에 할당
          if (emailCookie !== undefined) { // email 쿠키가 존재하면 
              setEmail(emailCookie); // email state에 email 쿠키값을 할당
              if (rememberEmailRef.current) {
                  rememberEmailRef.current.checked = true; // email 기억 체크
              }
          } else if (rememberEmailRef.current) {
              rememberEmailRef.current.checked = false;
          }
          
          // 패스워드 쿠키 존재 여부 확인 후 패스워드 쿠키가 존재하면 패스워드 state에 할당
          if (passwordCookie !== undefined) { // 패스워드 쿠키가 존재하면
              // password를 디코딩
              try {
                  const bytes = CryptoJS.AES.decrypt(passwordCookie, secretKey);
                  const decrypted = bytes.toString(CryptoJS.enc.Utf8);
                  setPassword(decrypted);  // password state에 디코팅 된 패스워드 쿠키 값 할당 
                  if (rememberPasswordRef.current) {
                      rememberPasswordRef.current.checked = true; // password 기억 체크
                  }
              } catch (error) {
                  console.log(error);
              }
          } else if (rememberPasswordRef.current) {
              rememberPasswordRef.current.checked = false;
          }
          
          // 자동로그인 쿠키 존재 여부 확인 후 자동로그인 쿠키가 존재하면
          // authkey 쿠키를 읽어 들여 서버로 비동기 전송
          if (authkeyCookie !== undefined) { 
       
              const formData = new FormData();
              formData.append("authkey", authkeyCookie);
              formData.append("autoLogin", "PASS"); // 예전 로그인 과정에서 저장된 authkey 쿠키로 로그인
  
              await fetch(`${apiUrl}/member/loginCheck`, {
                  method: 'POST',
                  body: formData
              }).then((response) => response.json())
                .then((data: LoginResponse) => {
                   if (data.message === 'good') {
                      navigate.push('/board/list?page=1');                      
                  } else if (data.message === 'expired') {					
                      navigate.push('/board/checkPasswordNotice');
                  } else {
                      alert("시스템 장애로 자동 로그인이 실패 했습니다.");
                  }		  
              }).catch((error) => { console.log(error); });
          }	
      }   
```

--------

### JWT(JSON Web TOKEN)와 Spring Security
- JWT : JSON 객체에 인증에 필요한 정보들을 담은 후 비밀키로 서명한 토큰으로, 인터넷표준인증방식 (인증(Authentication) 및 권한허가(Authorization) 용도로 사용)
  - JWT

  ##### * cf. 기존 인터셉터 방식과 로직은 비슷하나, 구조에 맞춰 작성하므로 안정성 높아짐

- Spring Security
 0) 로그인 체크 시 Spring Boot의 JSON Web Token 생성(Access Token, Refresh Token) - JWT 기반 인증(Stateless 인증)
 1) WebSecurityConfig.java 생성
    1) JWTAuthFilter 실행 :  hearder에 담겨있는 Authorization 정보 (Bearer 토큰) 확인
    2) JWTAuthFilter의 validate 메소드 이용하여 토큰 유효성 확인
    3) JWTAuthFilter의 getDataFromToken 메소드를 이용하여 토큰 내에 저장된 user 정보 확인
    4) UserDetails userDetails = useDetailsServiceImpl 을 통해 user 정보 획득
    5) UserDetailsImpl에서 획득한 사용자 정보를 UserDetailsImpl 생성자에게 전달
    6) UserDetailsImpl이 사용자 정보를 User로 전달

  ##### * User : Spring Security 의 최상위 고객 정보 관리 객체
  ##### * Filter : 어떤 프로그램이 실행하기 전이나 후에 반드시 실행하도록 하는 프로그램
  ##### * WebSecurityConfig.java의 SecurityFilterChain 객체를 통해 스프링 프로그램 실행 전에 접근 권한 확인 및 부여하는 절차 수행

--------

### * SOP, CORS, CSRF
- SOP(Same Origin Policy) : 같은 출처 내부에서만 데이터 교환 허용하도록 하는 웹사이트 규정
- CORS(Cross Origin Resource Sharing) : CORS 에러 해결을 위해 예외적으로 허용하는 규칙 - REST API에 CORS 설정 해줌
- CSRF(Cross-Site Request Forgery) : 사이트 간 요청 위조 -> 방지하기 위해 방어 코드 작성 : CSRF 토큰 사용 혹은 SameSite 쿠키 설정
  * CSRF 토큰 사용 예시(쿠키-헤더 방식) : 백엔드에서 CSRF 토큰 생성 -> 프론트엔드에서 헤더를 통해 전송 -> 서버에서 쿠키 속 토큰과 헤더 속 토큰 비교 및 검증으로 CSRF 방지
    
##### cf. 로컬 개발 시에는 프록시 방식을 통한 우회도 사용(프록시 서버를 통해 요청 주고받는 방식)

--------

### ResponseEntity
  - 백엔드가 클라이언트(프론트엔드)에게 HTTP 응답을 보낼 때, HTTP 상태 코드(Status Code), 응답 헤더(Response Header), 핵심 데이터(Body)를 개발자가 자유롭게 제어하고 조립해서 보낼 수 있게 해주는 스프링의 응답 전용 객체
  
  1. 데이터와 상태 코드를 함께 보내는 방식 (주로 사용)
   ##### ex ) ResponseEntity.ok().body(데이터)  
  2. 데이터 상태 코드만 보내는 방식
   ##### ex ) ResponseEntity.noContent().build(); //204 No Content
