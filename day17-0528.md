# Linux 실습

### 서버 연결 실습
- Apache2 + Tomcat9 연동

- openssl 설정

- SSH를 통한 Ubuntu(VM) 터미널 원격 접속(MobaXterm)

### Samba Client
- SMB 프로토콜 : 리눅스에서 사용 가능한 윈도우 파일/디렉토리 공유 프로토콜

#### Windows
1. 외부에서 윈도우즈 공유 폴더 접근시 로그인 할수 있는 윈도우즈계정 생성
 - 윈도우 홈 버튼을 마우스 오른쪽 버튼으로 클릭 > 컴퓨터 관리
 - 로컬사용자 및 그룹 > 사용자 > 오른쪽 버튼 클릭 > 새사용자 > root 계정 생성
2. 윈도우즈 파일탐색기에서 C:\smbShare  폴더 생성

#### Ubuntu
1. $sudo apt -y install samba-common smbclient cifs-utils
2. $sudo smbclient -L 윈도우즈_IP_주소 --> 공유 가능 여부 확인
3. 리눅스 내에서도 윈도우와 공유할 디렉토리 생성 $mkdir smbShare
4. sudo mount -t cifs -o user=root,password=12345 //192.168.0.112/smbShare  ./smbShare

### VM IP 고정 IP로 전환
1. 가상 머신 종료
2. 관리자 권한으로 PowerShell 실행
3. MySwith라는 가상 스위치(가상 네트워크 카드)를 생성
    ```powershell
     C:\>New-VMSwitch -SwitchName "MySwitch" -SwitchType Internal 
    ```
4. MySwitch에 192.168.20.1 할당 및 24비트 주소 대역 할당
     ```powershell
      C:\>New-NetIPAddress -IPAddress 192.168.20.1 -PrefixLength 24 -InterfaceAlias "vEthernet (MySwitch)"
     ```
5. 192.168.20.0/24 대역의 내부 IP를 NAT 처리
   ```powershell
    C:\>New-NetNat -Name "NATNetwork" -InternalIPInterfaceAddressPrefix "192.168.20.0/24"
   ```

### 파일 시스템
 - 컴퓨터에서 파일이나 자료를 쉽게 발견 및 접근 가능하도록 보관 또는 조직하는 체제로, 운영체제가 관리

#### 구조
- sector
- file
- directory
- block / cluster

##### cf. i-node

#### Linux 파일 시스템 종류
- FAT(File Allocation Table) : 저용량에 적합/보안성 낮음/호환성 좋음 (파일 최대 크기:4GB)
- NTFS(New Technology File System) : 사용자마다 다른 보안 적용 / 파일과 폴더에 개별 권한 설정 가능(실제 파일 최대 크기:2TB)
- exFAT(Extended File Allocation System):호환성 좋아 맥OS 에서도 읽기 쓰기 가능/안전성 낮음(파일 크기 무제한)

#### Linux 파일 시스템 종류
- EXT3
- EX4
- XFS
