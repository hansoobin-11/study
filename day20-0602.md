# CI/CD
 - CI (Continuous Intergration/지속적 통합)
   - 개발자들이 각자 수정한 코드를 하나의 중앙 저장소(ex : Git...)에 수시로 병합하는 과정
   - 코드가 합쳐질 때마다 자동으로 빌드(Build) 및 테스트(Test)가 이루어짐
 - CD (Continuous Delivery/Deployment/지속적 제공 및 배포)
   - 지속적 제공(Delivery) : 빌드 및 테스트가 완료된 코드를 리포지토리에 자동 업로드하는 과정(실배포 전 검증 단계까지 자동화)
   - 지속적 배포(Deployment) : 검증된 코드를 실제 운영 서버(Production)에 고객이 바로 사용 가능하도록 자동으로 배포하는 단계
  
## Jenkins
 - CI/CD 파이프라인을 구축하고 운영할 수 있도록 도와주는 오픈 소스 자동화 서버

### Jenkins의 특징
 - 플러그인 중심 구조 : Git, Docker, Kubernetes, AWS 등 여러 외부 도구와 연동 가능한 플러그인 제공
 - 파이프라인(Pipeline) : 스크립트 코드(Jenkinsfile) 형태로 빌드, 테스트, 배포 단계를 정의하여 복잡한 프로세스를 시각적으로 관리 가능
 - 분산 빌드(Master-Agent 구조) : 메인 서버(Master)가 작업을 총괄하고 여러 대의 Agent(또는 Slave) 서버가 실제 빌드 및 테스트 작업을 나누어 처리 -> 효율성 높아짐

### CI/CD 도입 효과
 - 코드 통합 : 코드 변경 사항이 있을 때마다 수시로 자동 통합 및 검증
 - 테스트 : 코드가 병합될 때마다 자동 테스트 스크립트 실행
 - 배포 과정 : 클릭 혹은 코드 푸시를 통해 운영 서버까지 자동 배포

##### cf. 마이크로 서비스 : 단일 애플리케이션을 작은 서비스의 집합으로 구축하는 설계 접근 방식(비즈니스 기능을 중심으로 구축)
