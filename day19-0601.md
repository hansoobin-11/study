# Docker 실습(2)

## docker volume
 - 컨테이너의 데이터를 영구적으로 저장하기 위한 방법

### docker volume 생성
1. docker create 이름 -> volume 생성
2. volume 할당
  2-1. 디렉토리 공유 방식
   - 컨테이너_01생성하면서 기존에 만들어진 볼륨에 컨테이너 디렉토리를 마운트
   - 컨테이너_02 생성하면서 기존에 만들어진 볼륨에 컨테이너 디렉토리를 마운트
     -> 컨테이너_01과 컨테이너_02가 같은 내용을 공유할 수 있음
  2-2. Bind Mount
   - 컨테이너_03 생성하면서 호스트 내의 디렉토리와 컨테이너_03의 디렉토리를 서로 공유
##### cf. 바인드 마운트(Bind Mount) : 컨테이너가 삭제되더라도 호스트에 남아 있는 데이터를 다시 연결 시키면 데이터 복구 가능

## docker image
 - 컨테이너를 변경해서 서비스 사용
 - docker의 환경 설정 및 데이터를 재사용하기 위해 서비스 중인 컨테이너를 이미지로 변환시켜서 저장
    - 이미지가 저장되는 유형 또는 저장소
      - 개인 또는 회사가 만든 사설 레지스트리
      - 도커 회사에서 운영하는 공인 도커 레지스트리(도커 허브)

### 컨테이너로 image 생성
1. commit 명령어로 생성
   ```cmd
     docker (container) commit 컨테이너이름 사설레지스트리주소:포트/생성할_이미지_이름:태그명(버전)
   ```
   
   ```cmd
     docker (container) commit 컨테이너이름 도커허브아이디/생성할_이미지_이름:태그명(버전)
   ```
   
2. Dockerfile 생성 후 build 명령으로 생성
   - Dockerfile 생성
   - index.html 파일 생성
   
   ```cmd
     docker build -t huahua10ve/apache_docker_image:1.0 .(dockerfile이 있는 폴더 경로)
   ```

   - 컨테이너 생성 및 실행
  
   ```cmd
     docker run --name apache_docker -d -p81:80 huahua10ve/apache_docker_image:1.0
   ```
## docker 사설 레지스트리 생성
```cmd
  docker run --name 레지스트리 이름 -d -p 5000:5000 --restart=always(옵션:재시작 시 자동 실행) registry
```

## Docker Compose

### docker-compose 설치(Linux)
1. docker-compose 다운로드
```cmd
  sudo curl -L "https://github.com/docker/compose/releases/download/v2.5.0/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
```

2. docker-compose 폴더에 실행 권한 부여
```cmd
   sudo chmod +x /usr/local/bin/docker-compose
```

3. 심볼릭 링크(바로가기) 설정
```cmd
  sudo ln -s /usr/local/bin/docker-compose /usr/bin/docker-compose
```
  - docker-compose 파일 내의 서비스 명을 docker DNS가 인식
  - docker의 네트워크 설정에서
    - Bridge : 내부에 가상 네트워크망을 추가
    - Host : 호스트와 동일한 레벨의 IP를 부여
      
4. docker-compose 버전 확인
```cmd
docker-compose --version
```

##### cf. yaml, yml : docker compose, kubernetes, application.properties(Spring Boot) 등 설정용 파일로 사용

### docker compose 실행
```cmd
 docker-compose up -d
```

## Kubernetes

### 구성 요소
- 마스터 노드 : 전체적인 제어 담당(컨테이너 관리)
- 워커 노드 : 실제 동작 담당(컨테이너 동작)
- 클러스터 : 마스터 노드 + 워커 노드

### 정의
 - 컨테이너 Orchestration 도구로, desired state를 유지하도록 상태(컨테이너)를 관리

### 주요 상태 관리 대상(Resource)
 - 파드(Pod) : 쿠버네티스에서 배포 가능한 가장 작은 단위
 - 서비스(Service) : 파드의 집합체로, 로드밸런서(부하분산장치) 역할
 - 레플리카세트(ReplicaSet) : 파드의 개수를 관리
 - 디플로이먼트(Deployment) : 파드의 배포를 관리

### 작성 방법
- 매니페스트(Manifest) : 파드나 서비스에 대한 쿠버네티스에서의 설정
  - 매니페스트 파일(정의 파일) : 매니페스트 기록 파일(YAML 또는 JSON 형식)
   - 주 항목 : apiVersion, kind(리소스 유형), metadata, spec(리소스 내용) 
     - spec 상세
        - replicas(작동하는 파드 숫자 설정), template(생성할  파드에게 부여할 메타데이터), nodePort(노드 포트로 서비스 간 연결 가능)

### 설치 방법
1. 설치 준비
```cmd
 sudo apt install -y apt-transport-https ca-certificates curl
```
2. 컨테이너 런타임 설치
3. 커널 모듈 및 네트워크 설정 : 리눅스 커널을 쿠버네티스 모드로 변경
4. kubeadm 과 직통으로 연결된 저장소 생성
