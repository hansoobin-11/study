ORM (객체-관계 매핑 : Object Relational Mapping)
: 데이터 베이스의 데이터를 클래스와 자동으로 연결(매핑) 해주는 것
 - 프로그램 내의 클래스와 DBMS 테이블 내의 데이터를 동기화
 - 데이터 조작을 통한 비즈니스 로직 구현은 클래스에 대한 API 사용
 
- 종류
 Hibernate - Spring JPA
 Node.js - Prisma

-------------------------

Typescript

: JavaScript + Type System

 - 정적 타입을 지원하며, 변수의 정의와 함께 대입할 값의 타입을 지정해야함

-------------------------

Spring Boot

- 프로젝트 생성 방법

  1. Spring Starter Project 

  2. Spring Initializer 
    * https://start.spring.io

- 사용 환경 설정

  - application.properties 에 설정값 입력
    * 내용 변경 시 재시작 권장
  - Gradle에서 외부 라이브러리 추가 및 삭제 : build.gradle 수정
     * 내용 변경 시 Gradle -> Refresh Gradle Project

front-end 는 React + Next.js / back-end는 Spring Boot / DB는 Oracle XE

 - DB 환경변수 설정
   DBMS_DRIVER : oracle.jdbc.driver.OracleDriver
   DBMS_PW : 12345
   DBMS_URL : jdbc:oracle:thin:@//localhost:1521/springpdb
   DBMS_USERNAME : springadmin

-------------------------

컴포넌트 함수 최상단에
const apiUrl = process.env.NEXT_PUBLIC_API_URL; 넣고 api 요청을 이 변수로 설정
ex) get(`${apiUrl}/member/login`)

-------------------------

WebSocket 

websocket 서버 구축
cmd 에서 서버 폴더 들어가서 node server.js 실행(해야지 웹소켓 서버 켜짐)
