# Jenkins, GitHub Action을 통한 CI/CD

## Jenkins

### Docker를 이용한 Jenkins 설치

  1. docker 명령(Ubuntu에서)
   ```cmd
  docker run -d --name jenkins \
  -v /home/xavier/.kube:/var/jenkins_home/.kube \
  -v /var/run/docker.sock:/var/run/docker.sock \
  -v /usr/bin/docker:/usr/bin/docker \
  -v $(which kubectl):/usr/bin/kubectl \
  -v /home/xavier/jenkins_home:/var/jenkins_home \
  -p 9000:8080 -p 50000:50000 \
  --group-add $(stat -c '%g' /var/run/docker.sock) jenkins/jenkins:lts
   ```
##### cf. jenkins:lts 의 내부 JDK는 21 버전 사용

  2. docker logs jenkins 명령으로 로그에서 Unlock 암호 확인 후 저장
  3. localhost:9000 주소로 접속(저장한 암호 사용)
  4. 플러그인 설치
  5. 교재의 JNLP(자바 웹스타터) 방식은 생략/Jenkins Permanent Agent(SSH 연결) 방식만 사용
  6. 개인키 가져와서 SSH 방식의 Agent Node 생성 및 Agent Node가 작업할 디렉토리 구성(ex : /home/milky/jenkins_project)
  7. docker 플러그인 설치
  8. 도커, 깃허브, 슬랙 보안 설정 등록

### jenkins project 디렉토리 구성 예시(gradle 버전 8.5 이상)

```
boardnext ----- .git                           
                       |     .gitignore
                       |     Jenkinsfile(젠킨스 설정 파일)
                       | - .github
                       |        | -- workflows
                       | - restapi
                       |        | --  
                       | - docker
                       |        | -- apache2
                       |        | -- boardrest
                       | - kubenetes 
                       |        | -- apache2.yml, boardrest.yml, ingress.yml, ...
```
### Jenkins 구성 시 생성되는 디렉토리 

 - jenkin_home --> Jenkins 설정 내용 저장
 - jenkins_board --> Agent working 디렉토리

### Jenkins 사용 범위

 - Staging Server == Test 서버용 CI/CD --> 실제 Production 환경에서의 CI/CD 60%
 - Workflow , BPM(Business Process Management) , RPA
 - Permanent Agent(SSH), docker/kubenetes 방식 --> in-docker, in-kubenetes 

## GitHub Action

 1. 프로젝트 디렉토리의 최상위에서 mkdir -p .github/workflows  생성
 2. workflows 디렉토리 내부에 deploy.yml
  --> 문법. 무조건 이렇게 만들어야 함
 3. deploy.yml 
  - runs-on: self-hosted --> 사용자(회사)의 서버에서 작동. 
   runs-on: ubuntu-latest --> GitHub에서 운영 중인 Ubuntu OS를 사용하는 서버에서 작동
 4. 환경변수 설정 
  - SLACK_WEBHOOK_URL 
   * slack에서 좌측 ...더보기 선택 > 도구 > 앱 > webhook으로 검색 > ...
 5. 로컬 서버(개인 또는 회사 서버)에 runner 설치
  - 개인별 Repository > settings > Actions > Runners
  - runner가 설치될 곳의 OS를 선택
  - download 및 configre 내의 GitHub 설명을 따라함. 
  - actions-runner 디렉토리가 생성됨
  - actions-runner 디렉토리로 이동한 후 아래 명령 실행 --> 계속 영구적으로 사용 가능
     - sudo ./svc.sh install 
     - sudo ./svc.sh start
 6. .gitignore 파일을 최상위 디렉토리에 만들고 여기에 actions-runner 추가 
     ===> actions-runner 내의 내용물들을 제외하고 GitHub로 저장
