managed language --> 언어 자체에서 Garbage Collection 수행 (C++/C#/Java/Pyhton)
unmanaged language --> 수작업으로 프로그램 작성시 메모리 제어를 해야 함(C언어)

[객체지향개념(OOP : Object Oriented Program)]
1. 캡슐화(Capsulization) : 클래스간 상호 침범 방지/독립화
2. 상속(Inheritance) : 클래스간 상호 데이터 및 네소드 공유가 필요할 때 
                            이거를 부모/자식 상하 계층 구조로 만들어서 관리
3. 다형성(Polymorphism) : 하나의 객체(상위 부모)로 다른 여러 객체를 
                                  조작할 수 있게 하는 기능
    예) 동물 클래스 (부모) -> 개, 호랑이, 오리 (자식) 클래스
              부모(동물)에서 자식들에게 "울어봐" 명령을 내리면 자식 클래스들은
              각자의 특성대로 명령을 수행  --> 의존성 주입 
4. 추상화(Abstraction--> 발췌, 요약) : 부모가 10개의 기능이 있는데 이걸 
       다 쓸 필요가 없을때 자식이 이 기능은 제가 가지고 있어니까 제걸로 할께요.   

[JDK Runtime Data Area]
- Method Area : 프로그램의 초기 작동을 준비하는데 필요한 데이터 
                      클래스정보, static 변수, 상수... --> 프로그램 종료시까지 생존
- Heap Area : 프로그램이 실행되는 중간중간에 생겼다가 없어지는 데이터들을 보관
                      인스턴스 정보 --> 공간이 꽉 차면 Garbage Collector가 자동으로 작동되어서 청소
- Stack Area : 프로그램이 실행되는 중간중간에 생겼다가 없어지는 데이터들을 보관
                    메소드내에서 사용하는 정보(매개변수, 지역변수)
 Heap --> FIFO(First In First Out) --> Queue(큐 --> 변발)
 Stack --> LIFO(Last in First Out) 

[소프트웨어공학 --> 프로그램 개발을 얼마나 잘 하느냐]
1. 프로그램 설계 관점 --> 
  디자인 패턴 --> "소프트웨어 설계 시 사용하는 디자인 패턴에 대해 설명"
  1.1 생성패턴 : 객체의 생성 방식을 결정하는 패턴
    - 싱글톤(Singleton) 패턴 : 클래스의 인스턴스가 단 하나만 생성
    - 팩토리 메소드(Factory Method) 패턴 : 인터페이스를 생성 
      --> 구현 시 인터페이스를 상속해서 실제 구현 가능한 메소드를 생성
    - 추상 팩토리(Abstract Factory) 방식 : 연관된 객체들을 모아서 제품군을 생성
    - 빌더(Builder) : 복잡한 객체의 생성 과정을 단계별로 분리하여, 동일한 생성 절차에서 
                         서로 다른 표현을 만들수 있게 함. 
   1.2 구조 패턴
     - 어댑터(Adaptor)
     - 데코레이터(Decorator)
     - 프록시(Proxy)
     - 퍼사드(Facade) 
   1.3 행위 패턴
     - 옵저버(Observer) : 이벤트 리스너, 발행-구독 
     - 전략(Strategy) : 
     - 템플릿 메서드(Template Method) : 부모가 설계 -> 실제 구현은 자식이...
     - 상태(State) : 

[함수형 프로그래밍]
1. 정의 : 파이프라인처럼 앞선 메소드의 결과를 이어 받아 처리를 하는 프로그래밍 패러다임으로 자바의 경우
   8버전부터 함수형 프로그램의 특성을 가진 stream API를 도입
2. stream API의 정의 및 등장 배경
 - 자바에서는 많은 양의 데이터를 저장하기 위해서 배열이나 컬렉션을 사용
 - 이렇게 저장된 데이터에 접근하기 위해서는 반복문이나 반복자(iterator)를 사용하여 매번 새로운 코드를 작성해야 함.
 - 하지만 이렇게 작성된 코드는 길이가 너무 길고 가독성도 떨어지며, 코드의 재사용이 거의 불가능
 - 즉, 데이터베이스의 쿼리와 같이 정형화된 처리 패턴을 가지지 못했기에 데이터마다 다른 방법으로 접근해야만 했음.
 - 이러한 문제점을 극복하기 위해서 Java SE 8부터 함수형 프로그램의 특성을 가진 스트림(stream) API를 도입
 - 스트림 API는 데이터를 추상화하여 다루므로, 다양한 방식으로 저장된 데이터를 읽고 쓰기 위한 공통된 방법을 제공
 - 따라서 스트림 API를 이용하면 배열이나 컬렉션뿐만 아니라 파일에 저장된 데이터도 모두 같은 방법으로 처리가 가능
3. stream API의 특성
 - 스트림은 외부 반복을 통해 작업하는 컬렉션과는 달리 내부 반복(internal iteration)을 통해 작업을 수행.
 - 스트림은 재사용이 가능한 컬렉션과는 달리 단 한 번만 사용.
 - 스트림은 원본 데이터를 변경하지 않음.
 - 스트림의 연산은 필터-맵(filter-map) 기반의 API를 사용하여 지연(lazy) 연산을 통해 성능을 최적화.
 - 스트림은 parallelStream() 메소드를 통한 손쉬운 병렬 처리를 지원. 
4. Stream API 동작 흐름
   데이터 소스 --> 스트림 생성 --> 중개연산(필터) --> 중개연산(맵) --> 최종연산(출력)

[웹서비스 시스템]
1. Apache HTTP Server 구성
 - 가상 호스팅
 - TLS/SSL 구성을 위해서 개인키, 인증서 생성 
 - HTTP/2.0 구현
2. Apache Tomcat WAS 구성
3. Proxy란?
4. HTTP Server와 Tomcat WAS간의 연동
 - mod_jk.so 라는 모듈을 다운 받고 아파치의 모듈 디렉토리에 설치 후 활성화
 - Proxy 방식 : 웹서버 --> WAS 다이렉션 시킴
5. Tomcat 서버를 엔진과 인스턴스로 분리하고, 인스턴스를 여러개 생성하여
   로드밸런싱을 구현 --> 
   - 로드밸런싱 상태에서 세션의 영속성을 유지하기 위한 방법
     * sticky mode : 한번 접속한 WAS에만 계속 접속하도록 하는 방법
     * session cluster : session을 여러 WAS로 전파/공유
     * DBMS에 session을 저장
     * 토큰을 이용하는 방법 --> stateless 방식. JWT(Json Web Token)

[스프링 구성]
0. MVC 디자인 패턴(교재 140 페이지)
 - MVC Model 1
 - MVC Model 2 
1. 스프링빈 생성을 위한 설정
 - IoC : Inversion of Control(제어의 역전)--> 프레임워크는 프로그램이 사람한테 지시
 - ApplicationContext 클래스 : BeanFactory라는 Factory 패턴 
     인터페이스의 구현체(상속)로 스프링빈들을 생성하는 
     스프링 빈 객체들의 최상위 클래스
 - Spring Bean : 스프링 시작시 ApplicationContext 객체가
    생성해서 Spring IoC Container라는 스프링 메모리 공간에 저장하는
    Singleton 패턴의 객체
    --> Spring Bean 생성 방식 
           ** XML 이용, 
           ** 자바 Configuration 객체 내부에 @Bean Annotation을 붙인 메소드에서 클래스를 리턴 
           ** @Component 이용(스프링빈 설정 XML 파일에서 스캔)
    --> 스프링빈 생성 XML 파일의 위치--> web.xml에 기록(src/main/webapp/WEB-INF)
2. MyBatis 설정
        classpath === src/main/resources
    --> 설정 XML 파일 위치 : src/main/resources/mybatis-config.xml
    --> Mapper XML 파일 위치 : src/main/resources/mappers/xxxMapper.xml
           ※ classpath:mappers/**/*Mapper.xml --> Ant 표기법
 - DTO(Data Transfer Object) : 스프링 객체들이 생성하거나 가지고 온 데이터들을 저장하는 객체
 - VO(Vale Object) : 최종 결정된 값들을 저장(Read Only)
3. Spring MVC 동작순서 --> Dispatch 서블릿의 흐름(교재 260 페이지)
4. 의존성 주입 
- 의존성 주입하는 방법
   * setter 메소드
   * 생성자 --> 권장
       public BoardController(BoardService service) {
	  	this.service = service; 
       }
   * Annotation 이용
       @Autowired
       private BoardService service; >> 인터페이스 인스턴스변수
           ==> BoardService 인터페이스를 상속해서 구현한 BoardServiceImpl 클래스를 스프링빈으로 생성
                  이렇게 생성된 스프링빈을 의존성 주입 방법으로 호출
5. 자바 및 스프링 관련 예상 질문
 - 스프링빈
 - 의존성주입
 - Dispatcher Servlet 작동 절차
 - Model vs ModelAndView
 - session vs cookie
 - AJAX
 - REST vs RESTfull vs REST API
 - MVC란? MVC Model 1, MVC Model 2
 - Desgin Pattern
 - 영속성 관리
   ** SQL Mapper : My Batis
   ** ORM : Spring JPA, Prisma(node.js 계열에서 사용)

[자바스크립트]
- 호이스트
- 클로저
- 일급 함수, 고차함수, 콜백함수
- 얇은 참조, 깊은 참조

[DBMS]
 - Mariadb(MySQL) 
 - Oracle 21c XE 

[Spring Security]
* Spring Security 작동 구조 
0. 로그인 체크 시 : 스트링부트의 /api/member/loginCheck에서 JSON Web Token 생성. 
   --> Access Token, Refresh Token ==> JWT 기반의 인증 --> Stateless 인증
1. WebSecurityConfig.java 생성
   --> Filter? : 어떤 프로그램이 실행하기 전이나 후에 반드시 실행토록 하는 프로그램    A --> B --> C
   --> WebSecurityConfig내의 "SecurityFilterChain" 객체를 통해 스프링 프로그램 실행전에 접근 권한을 확인하여 부여하는 절차를 수행
   1-1. JWTAuthFilter 실행 <-- headers: { Authorization: `Bearer ${accessTokenCookie}` } 확인
   1-2. JWTAuthFilter의 validate 메소드를 이용하여 토큰 유효성 확인
   1-3. JWTAuthFilter의 getDataFromToken 메소드를 이용하여 토큰내에 저장된 email을 읽어 들임
   1-4. UserDetails userDetails = userDetailsServiceImpl.loadUserByUsername(email); --> email을 userDetailsServiceImpl에 넣어 "사용자 정보를 획득"
   1-5. UserDetailsImpl에서 획득한 사용자 정보를 UserDetailsImpl의 생성자에게 전달
   1-6. UserDetailsImpl 객체는 사용자 아이디, 패스워드, Role 값등을 Spring Security 의 최상위 고객정보 관리 객체인 "User 객체로 전달" 

* ORIGIN, CORS, CSRF 
1. Same Origin Policy(SOP) : 같은 출처 내부에서만 데이터 교환이 허용됨
2. CORS (Cross Origin Resource Sharing) : 출처(Origin)가 다른 것끼리 데이터 교환을 금지하는 규정 --> 이 규정을 집행/실행 하는 장소가 웹브라우저
 http://localhost:3000/board/list 내부 코드에서  fetch(`${apiUrl}/board/list?page=${page}&keyword=${keyword}을 실행하면 
 웹브라우저 입장(클라이언트에서 실행되는 부분. )에서는 서로 다른 출처끼리 데이터가 교환되는 걸로 인식이 되기 때문에 
 런타임시 에러 발생...  
---> 회피 방법 
 -- 2.1 이러한 규정을 피하기 위해서는 최소 한쪽에서 이 규정을 적용하지 말라는 코드를 작성해서 통지... Access-Control-Allow-Origin 필드에 허용할 도메인을 
    적어서 보내 주는 방법  --> REST API에서 작성
3. CSRF(Cross-site Request Forgery) 
 3-1. 사용자가 은행 사이트(www.bank.com)에 로그인 --> 쿠키를 브라우저에 남겨 둠
 3-2. 해커가 사용자에게 이메일을 보냄 --> 이메일 내용 중에 뭔가를 클릭하도록 유도 
 3-3. 만약에 클릭을 하게 되면 이상한 코드가 작동
     <a href=http://www.bank.com/transfer?to=hacker&mount=1000000>만남</a>
 3-4. 로그인에 필요한 정보를 담은 쿠키가 웹브라우저에 저장 되어 있기 때문에
       해커가 작동시킨 프로그램이 그 쿠키를 읽어서 로그인하고 작동
 3-5. 이런 불법적인 활동을 막기 위한 보안기술이 CSRF
 - 서버가 CSRF 토큰을 발행. 서버에 요청시 이 토큰을 항상 같이 가져오게 해서 토큰 진위를 판단하게 함
 - CSRF를 사용하려면 백엔드로 스프링시큐리티를 적용하고 프론트엔드쪽에서 Form내에 서버에서 
   보내 온 토큰을 적용할 수 있도록 프로그래밍을 해야 함...
<form action="/update-profile" method="POST">
    <!-- 서버가 발급하여 세션에 저장한 고유한 CSRF 토큰 -->
    <input type="hidden" name="csrf_token" value="abc123xyz789">
    
    <label>이메일:</label>
    <input type="email" name="email" value="user@example.com">
    
    <button type="submit">저장</button>
</form>


[React + Next.js]
<React의 특성>
1. JSX(Javascript XML) 문법 사용
 - 자바스크립트 ES6 문법을 기반으로 확장된 스크립트 언어인 JSX를 학습하기 위해선 ES6 문법에 대한 이해가 필수
 - Node.js의 V8 엔진과 NPM(Node Package Manager)를 이용하는 관계로 Node.js에 대한 기초지식도 필요
   V8엔진 --> 크롬 웹브라우저에 내에 설치되어 있는 자바스크립트 번역기(interpreter). 
                   이걸 끄집어 내어서 웹브라우저 바깥에서 자바스크립트를 일반 프로그램 언어로 사용가 능하도록 만듬
2. 컨포넌트간 데이터 전달은 단방향 데이터 바인딩(One-way Data Binding)을 사용.
    최근에는 zustand, redux 등의 전역 데이터 관리 라이브러리를 활용하여 전역 데이터 관리
3. 가상돔(Virtual Dom) 사용
4. 프로젝트 관리를 위해 npm 을 사용. 이번 과정에선 npm 대신 yarn 사용.

<가상돔 이란???>
- 웹브라우저가 HTML을 읽어서 DOM Tree를 메모리상에 로딩. 
- 메모리에서 작동 중인 자바스크립트(자바스크립트 생성 언어가 리액트든 제이쿼리든 최종적으로 배포되어서 
  사용자 웹브라우저로 전송되는 건 자바스크립트)가 이 DOM Tree와 동일한 구조를 갖는 가상돔(Virtual DOM)을 생성  
- 이후에는 웹브라우저에서 작업을 할때 가상돔에서 작업을 하고 여기서 바뀐 내용을 실제 DOM에 적용...

<React를 사용 시 클라이언트(웹브라우저)와 서버(웹서버) 간 데이터 이동 및 수행 되는 업무>
1. 서버에 최초 요청 ==> 클라이언트의 웹브라우저
2. 서버에서 요청에 따라 자바스크립트 파일과 HTML을 보내 줌 ==> 서버에서 수행
==> 여기서부터는 전부 클라이언트(사용자 웹브라우저)에서 수행
3. 클라이언트 웹브라우저에서 서버로부터 받은 자바스크립트 파일을 실행
4. HTML 및 DOM과 Virtual DOM을 만듬
5. Virtual DOM --> DOM에 변경된 내용을 집어 넣고 DOM을 통해 HTML을 생성
 ===> 5번 단계에서 수행되는 걸 "Hydration" --> HTML과 자바스크립트가 결합
5.1 Hydration이 일어나서 HTML이 메모리에 로딩되는 단계 --> 마운트(Mount)
6. 웹브라우저가 생성된 HTML을 읽어서 웹브라우저 화면에 렌더링

※ 리액트는 "상태(state) 관리"를 통해 "SPA(Single Page Application)을 구현"
    리액트 프로그램 내에서 연산이 끝나고 상태 변화에 상관 없이 보관이 필요한 데이터는 
    가급적 Ref 객체에 저장하는 것이 안전하고 시스템 자원 관리에도 용이함.

※ 자바스크립트 계열(node.js React, Vue 등등)에서 전역 데이터 관리 도구로 
   현재 사용되는 것들 --> redux, redux toolkit, zustand, pinia...
   --> 실제 실무에선 REST API를 호출해서 값을 저장하거나 저장된 값을 불려 사용게 편함.

<렌더링 구현 방법>
CSR(Client Side Rendering) : 사용자 웹브라우저 내에서 
   모든 HTML을 자바스크립트가 만들어서 화면에 렌더링  
   장점 : 서버 부하를 줄이고, 클라이언쪽에서 바로 실행되고 렌더링을 최소화 하기 때문에
            화면 전환 속도가 빠르다...
   단점 
    - 초기 화면 로딩을 하는데 시간이 걸린다. 
    - 검색엔진이 웹사이트 검색하는게 어렵다...

SSR(Server Side Rendering) : 서버에서 HTML을 가공한 후 이걸 클라이언트에서 받아서 처리
 검색엔진에 노출이 필요한 부분은 서버에서 HTML로 가공 후 클라이언트로 전송..

SSG(Static Site Generation) : 서버에서 빌드 순간에 만들어진 내용을 그대로 HTMl로 전환해서
 클라이언트로 전송 
 단점 : 실시간 정보를 다루는 사이트에 사용해선 안됨...
--> 전략을 잘 세워서 SEO(Search Engine Optimization : 검색엔진 최적화)에 적합한 웹페이지를 구성

<기타>
 - SPA(Single Page Application) 
 - Front-end 프로그램이란??  SPA를 구현하게끔 만들어 주는 프로그램
 - "이벤트 기반 싱글스레드 논블로킹 I/O 모델"

 - Next.js --> 파일 기반 라우팅, SSG로 배포용 결과물 생성하는 법

[DevOps]
1. 가상화 이론
 - Type1 , Type2
2. 클라우드 서비스 
 - IaaS, PaaS, SaaS, On-Premise, Serverless
3. Dev/Ops
 - 배포전략 : 롤링, 블루그린, 카나리
4. 리눅스의 구조
 - Kernel, Shell, Application
 - Shell 종류
 - OS에 대한 기본 지식
5. 도커 & 쿠버네티스
 - 정의 및 사용 목적, 주요 기능
