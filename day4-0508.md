람다 표현식(Lambda Expression)

 - 이름 없는 함수(익명 함수)를 만들기 위한 문법

 - 익명성 : 함수의 이름이 없음

 - 간결성 : 불필요한 표기를 줄임

 - 일급 함수(First Class Function)
  1) 변수에 할당이 가능하다 : 함수 표현식으로 작성 가능
  2) 다른 함수를 매개변수로 받을 수 있다
  3) 다른 함수의 결과로 반환 가능하다

 - 고차 함수(High Order Function)
  - 일급 객체의 특성 중 2, 3번의 특성을 가진 함수

 - 콜백 함수(Callback Function)
  - 일급 객체의 특성 중 2번의 특성을 가진 함수

 - 문법 구조

	```   
	매개 변수 -> { 함수 몸체 }
	//->(화살표) : 매개변수와 몸체를 구분한다

	````
 - 함수형 프로그래밍, 데이터 처리 관련 프로그래밍에서 사용하는 자바 외부 라이브러리들이 람다 표현식을 많이 사용한다
 * 람다 표기법 사용 언어 : 자바, 자바스크립트(화살표 표기법), 파이썬

 - 사용법
  - @FunctionalInterface 어노테이션을 사용하여 익명 함수로 만드는 법 : 사용자 정의
  - 기존에 만들어진 인터페이스들을 호출해서 사용하는 법
    - Supplier<T> : 매개 변수 없이 반환 값 만을 받는 함수 인터페이스
      - 실행 메소드 : get()

    - Fuction<T,R> : 매개 변수 T를 받아서 연산 후 결과 R을 반환
      - 실행 메소드 : apply()

    - Consumer<T> : 매개 변수 T를 받아서 소비(사용)만 하고 반환하는 값이 없음(void 반환)
      - 실행 메소드 : accept()

      * chainning 기법
        - ex ) andThen 
               - 1. accept 메소드를 통해 인자를 받고 명령을 사용(소비)
               - 2. accept 에서 받은 인자를 가지고 실행할 명령으로 전달하여 실행

    - Predicate<T> : 매개 변수 T를 받아서 처리한 후 Boolean을 반환
      - 실행 메소드 : test()

    - 메소드 참조 : 람다 표현식이 단순히 기존에 있는 메소드를 호출하기만 할 때 사용하는 방식(매개 변수 전달 과정 생략)
      - 표현 방식 

        ```
   	클래스 이름 :: 메소드 이름 또는 인스턴스 변수 이름 :: 메소드 이름
        ```
        - DoubleUnaryOparator
           - 한 개의 double형 매개 변수를 전달받아 한 개의 double형 값을 반환하는 java.util.fuction 패키지에서 제공하는 함수형 인터페이스
           - 실행 메소드 : applyAsDouble

     - 생성자 참조
    

----------------------------------------

함수형 프로그래밍 특징

- 순수 함수(Pure Function) : 외부의 변화가 함수 내부에 영향을 미치지 않음
- 비상태, 불변성(Stateless, Imuutability) : 함수 호출로 외부의 상태가 변경되지 않음
- 제어문(조건문, 반복문 등) 을 사용하지 않고 전용 함수 사용(ex : map() 등)
- 일급 함수

----------------------------------------

Stream API

* 등장 배경 
   ```
    자바에서는 많은 양의 데이터를 저장하기 위해서 배열이나 컬렉션을 사용하여 매번 새로운 코드를 작성
     -> 이렇게 작성된 코드의 길이가 너무 길고 가독성이 떨어지며 코드의 재사용이 거의 불가능
     -> 데이터베이스의 쿼리와 같이 정형화된 처리 패턴을 가지지 못함 -> 데이터마다 다른 방법으로 접근해야하는 단점
     => 이러한 문제점을 극복하기 위해 자바 8 버전부터 함수형 프로그래밍의 특성을 가진 stream API를 도입
     stream API는 데이터를 추상화하여 다루므로, 다양한 방식으로 저장된 데이터를 읽고 쓰기 위한 공통된 방법을 제공
     -> 배열이나 컬렉션 뿐만 아니라 파일에 저장된 데이터도 모두 같은 방법으로 처리가 가능
   ```

- 특성
 1. 외부 반복을 통해 작업하는 컬렉션과 달리 내부 반복(internal iteration)을 통해 작업을 수행
 2. 재사용이 가능한 컬렉션과 달리 단 한번만 사용
 3. 원본 데이터를 변경하지 않음
 4. 필터-맵(filter-map) 기반의 API를 사용하여 지연(lazy) 연산을 통해 성능을 최적화
 5. 병렬 처리를 지원 (파이프라인)

- 작동 순서

   ```
    데이터 소스 확보 -> 스트림 생성 -> 중개 연산(필터) -> 중개 연산(맵) -> 최종 연산(출력)
   ```

- 스트림 생성
  - 자바에서 제공하는 모든 컬렉션의 최고 상위 객체인 Collection 인터페이스에는 stream() 메소드가 정의되어 있음
  - Collection 인터페이스의 구현체인 List와 Set 컬렉션 클래스에서도 stream() 메소드를 생성할 수 있음
  - 배열을 다루는 Arrays 클래스에서도 다양한 형태의 stream() 메소드가 정의되어 있음
  - 기본 데이터 타입인 int, long, double 형을 다룰 수 있는 배열에 관한 스트림이 별도로 정의되어 있음
    - java.util.stream 패키지의 IntStream, LongStream, DoubleStream 인터페이스로 각각 제공

- 스트림 중개 연산
  - 스트림 API에 의해 생성된 초기 스트림은 중개 연산을 통해 또 다른 스트림으로 변환
  - 스트림 결과값을 연결하여 받은 후, 결과값을 다시 재가공해서 넘겨주는 방식 -> 연결을 통한 데이터 연산이 가능
  - 스트림의 중개 연산은 필터-맵(filter-map) 기반의 API를 사용함으로써 지연(lazy) 연산을 통해 성능을 최적화

   -종류
    1. 스트림 필터링 : filter(), distinct()
    2. 스트림 변환 : map(), flatMap()
      - map() : 각 요소를 1:1 변환
      - flatMap() : 각 요소를 변환한 뒤, 내부 Stream을 하나로 병합(flatten)
    3. 스트림 제한 : limit(), skip()
      - limit() : 해당 스트림의 첫번째 요소로부터 전달된 개수만큼의 요소들로 이루어진 새로운 스트림을 반환
      - skip() : 해당 스트림의 첫번째 요소로부너 전달된 개수만큼의 요소들을 제외한 나머지들로 이루어진 새로운 스트림을 반환
    4. 스트림 정렬 : sorted()
    5. 스트림 연산 결과 확인(디버그) : peek()
      - 결과 디버깅용
      - 스트림 소비 X
      - 최종 연산으로 사용하면 작동X

- 스트림 최종 연산
  - 종류
     1. 출력 : forEach()
     2. 소모 : reduce()
         - 누적된 값을 계산
     3. 검색 : findFirst(), findAny()
     4. 검사 : anyMatch(), allMatch(), noneMatch()
     5. 통계 : count(), min(), max()
     6. 연산 : sum(), average()
     7. 수집 : collect() 
        -필터링 또는 매핑된 요소들을 새로운 컬렉션에 수집하고 반환(joining, sorting 기능도 있다)

cf. 지연(lazy) 연산 : 필요한 연산만 효율적으로 수행하는 것

----------------------------------------

JDBC

- MariaDB 사용
 1. MySQL Client를 이용하여 root 계정으로 로그인
 2. 데이터 베이스 생성
  ```
 	$create database webdev;
  ``` 
 3. 사용자 계정 생성
  ```
	      계정 이름       위치                     패스워드
  	$create user 'webmaster'@'localhost' identified by "1234";
  ``` 
 4. 사용자 권한 설정
  ```
  	$grant all privileges on webdev.* to 'webmaster'@'localhost';
  ```

- DB 연결
 - JDBC 를 통한 DB 연동 예제 공부
