# Linux 실습(2)

### 명령어 모음
- curl : 서버와 데이터를 송수신할 때 사용
  - 웹 서버 접속, API 테스트, 파일 다운로드 등
- sed : CLI 상에서 파일을 편집하는 프로그램
- tar : 파일 패키징/압축 해제 시 사용
  - tar -czvf : 압축 생성
  - tar -xzvf : 압축 해제

### FTP(File Transfer Protocol)
 - vsftpd 설치 후 설정 파일 변경

### 가상 디스크 용량 확장
- 디스크 편집에서 확장 설정 후 gparted로 디스크 할당
# Docker
- Linux Container(LXC) 기술을 기반으로 만들어진 컨테이너 관리 도구

### Docker 설치
- Window : WSL 설치 후 Docker Desktop 설치
- Ubuntu : 도커 엔진 설치 후 사용 중인 계정을 도커 그룹에 포함

### Docker 명령어 구조
- docker image 커맨드
- docker 단독 커맨드
- container 명령어
 - 컨테이너 생성 명령 예시
  ```cmd
    docker (container 생략 가능) run --name 컨테이너명 -d -dit -p 사용할 포트번호:기존 포트번호 사용 이미지명
  ```
- network 명령 : 여러 개의 container를 묶어 서로 연동된 서비스 구성
  - 생성 명령
  ```cmd
    docker network create myboard --gateway 172.20.0.1 --subnet 172.20.0.0/24
  ```
  ##### cf. subnet ip주소는 172.20.1.1 부터 172.20.254.254 까지 자동 할당

### Docker를 통한 3-Tier 웹서비스 구현 : apache 웹서버 + tomcat WAS + mariadb
0. hosts 파일에 가상 도메인 설정
1. 네트워크 생성
2. 컨테이너 생성(apache,tomcat,mariadb)
3. 컨테이너 별 환경 설정
  - apache : httpd.conf / httpd-vhosts.conf 설정
  - tomcat : server.xml 설정
  - mariadb : 테이블 생성
 4. 컨테이너 내부의 설정 파일들을 교체

##### cf. docker를 이용한 배포는 Port Forwarding 방식
