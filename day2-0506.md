IDE(Intergrated Development Environment/통합개발환경)

- Visual Studio Code : MS
- IntelliJ : Jet Brain (Kontlin 언어, Android Studio)
- Eclipse : Java
- Spring Tool Suite(STS) : Eclipse에서 Spring 프로젝트 개발용으로 출시

-----------

JDK(Java Development Kit) 설치 방법


1. Oracle 또는 기타 OpenJDK 제공사 홈페이지에서 JDK 다운로드 및 설치(LTS-Long Term Support : 8, 11, 17, 21, 25 중 다운로드 권장)

2. 환경변수 설정

- Window
  - 1) 설정 -> 고급 시스템 설정 -> 환경 변수 -> 새로 만들기 -> JAVA_HOME - jdk 폴더 경로 설정
  - 2) Path 등록 - Path 편집 -> 새로 만들기 -> %JAVA_HOME%\bin
  - 3) CLASSPATH 등록 - %JAVA_HOME%\lib;.;
  - 4) 명령프롬프트로 확인

- Linux (추후 세팅)

-----------

Java Project 구성

- Eclipse 기준
  - 전체 작업 폴더(workspace) 생성
  - workspace 내부에 project 폴더 생성(구조화)
  
* 네이밍(Naming) 규칙
 - 대소문자 구분
 - 숫자로 시작 불가
 - 예약어 사용 불가(ex : int...)
 - CamelCase 사용
  //cf. SnakeCase : _(언더바)로 구분하는 표기법
 - 워크 스페이스/프로젝트명 - 시작문자는 대소문자 모두 가능하나 첫 문자를 포함한 모든 단어는 대문자로 시작하도록 권장
 - 클래스 : 명사 대문자로 시작 권장
 - 메소드 : 동사 소문자로 시작 권장

Java Project 환경

- 적합한 폴더(dir) 구성 -> 서비스 배포 환경을 구성
   WEB-INF 
     L src
     L classes
     L lib

- 컴파일 시 적합한 옵션(인코딩, 경로 인식 등...) 설정


-----------

Java 프로그래밍

- CLI(Command Line Interface) -> TUI(Text User Interface)
  - prompt에 텍스트로 명령 입력
  - history 기능 : 화살표키 상하 이동으로 과거 입력했던 명령어 호출
  - shortcut 기능 : 시작 단어 + tap 키를 이용한 단축키 기능

- GUI(Graphic User Interface)

javac -파일명.java : 컴파일 명령
java -파일명.java : 자바 파일 실행

* 949 인코딩 에러 해결
 - javac -encoding UTF-8 파일명.java -d(디렉토리 설정) 폴더명

* package 기반 java class 파일 실행

 - class 파일이 있는 폴더에서 java 패키지경로(ex : com.test.hello.HelloWorld)

-----------

개념 정리

managed language : 언어 자체에서 Garbage Collection 수행 (C++/C#/Java/Python)
unmanaged language - 수동으로 프로그램 작성 시 메모리 제어해야함 (C)

변수(Variable) 
  - 하나의 값을 저장하기 위해 확보한 메모리 공간 자체 또는 그 메모리 공간을 식별하기 위해 붙인 이름

선언(declaration) : 컴파일러에게 변수명을 알리는 것(ex : int a;)
  - 로딩(로딩이 되어 실행 가능한 상태가 된 것은 process) 시 메모리에 공간이 확보됨

할당, 대입, 저장(assignment) : 변수에 값을 저장하는 것(ex : a = 1;)
  - 프로세스가 실행(Runtime) 중에 수행하는 명령

참조(reference) : 변수의 값을 읽어들이는 것


스코프(Scope)
  - 변수에 접근할 수 있는 유효 범위/영역으로, 변수의 사용 가능한 범위를 지칭

라이프타임(lifetime) : 변수가 메모리 내에서 존재할 수 있는 기간

인스턴스 변수(Instance Variable)
 - 클래스 내부와 모든 메소드 및 블록 외부에서 선언된 변수
 - 스코프 : 정적 메소드를 제외한 클래스 전체
 - 라이프타임 : 객체가 메모리에 존재할 때 까지

클래스 변수(Class Variable)
 - 클래스 내부, 모든 외부 블록에서 선언되고 static으로 표시된 변수
 - 스코프 : 클래스 전체
 - 라이프타임 : 프로그램이 종료될 때 까지

로컬 변수(Local Variable)
 - 인스턴스 및 클래스 변수가  아닌 모든 변수
 - 스코프 : 선언된 블록 내
 - 라이프타임 : 컨트롤이 선언된 블록을 떠날 때 까지

일반형(원시형, Primitive Type)
 - 변수명으로 할당된 메모리 공간에 실제 값이 존재
   - 정수형(int,short,byte,long)
   - 실수형(float, double)
   - 문자형(char)
   - 논리형(boolean)

참조형(Reference Type)
 - 실제 값이 아닌 배열로 구성된 메모리 주소가 존재(기계어 코드로 구성)
   - 열거형
   - 클래스형
   - 인터페이스형

cf. 추론형(ex : var) : 런타임 시 변수 타입이 지정 (권장 x)

-----------

조건문(Condition Statement)

제어문/반복문(Iteration Statement)

-----------

VScode에서 no build tool 로 자바 프로젝트 생성

- 자바 확장팩 설치
- 폴더 선택 후 프로젝트 생성 -> 패키지 생성 -> 클래스 생성

-----------


객체지향프로그래밍(OOP, Object Oriented Program)

 - 특성

  1. 캡슐화(Capsulization) : 클래스 간 상호 침범 방지/독립화

  2. 상속(Inheritance) : 클래스 간 데이터 및 메소드 공유 필요할 경우, 부모-자식 상하 계층 구조로 만들어서 관리

  3. 다형성(Polymorphism) : 하나의 객체(상위 부모)로 다른 여러 객체를 조작할 수 있도록 하는 기능(의존성 주입) 
	- 오버라이딩(Overriding, 런타임 다형성) : 부모 기능을 자식이 덮어쓰는 것
	- 오버로딩(Overloading, 컴파일 타임 다형성) : 같은 이름이지만 다른 입력값을 가지는 것

  4. 추상화(Abstraction/발췌, 요약) :  복잡한 세부사항은 숨기고 필수 특징만 드러내는 것


객체(Object)
 - 주체와 주체가 행하는 행위
 - OOP(객체 지향 프로그래밍) 에서는 객체지향 설계를 통해 객체를 캡슐화

클래스(Object)
 - 객체를 프로그래밍 코드로 정의
  - 인스턴스(Instance) 변수(전역/멤버 변수) : 주체의 속성을 변수로 정의
  - 메소드 : 주체의 행위를 정의

ex ) 

class MinsooTodolist { //민수를 프로그래밍화하기 위해서 필요한 것들을 모아 놓은 코드(틀)
	int age; //인스턴스(전역/멤버) 변수
	int height;
	int payment;

	void wake(){} //메소드
	void eat(){}
	}

//주체를 각각의 상황에 따라 표현(인스턴스/각각의 상황)

MinsooTodolist workingDay = new MinsooTodolist();
MinsooTodolist holiday = new MinsooTodolist();


  - MinsooTodolist : 클래스
  - workingDay, holiday : 인스턴스 변수, 생성자를 통해 만들어진 클래스가 존재하는 공간의 주소를 할당
  - new : 인스턴스를 생성하게 해주는 자바 지시어
  - MinsooTodolist() : 생성자(Constructor)

-----------
 
캡슐화 관련

- 접근제한자

 - 외부 코드에서의 접근(사용) 범위를 통제(정보 은닉이라는 객체 지향 언어의 특성)

 - 직접 인스턴스 변수 값에 접근하기보다 게터/세터를 통해 간접적으로 접근하는 것이 권장된다

    - public
    - private
    - protected
    - default


- 지시어(키워드)

  - static : 공용으로 사용되는 메소드나 인스턴스 변수 앞에 선언(객체 없이 사용 가능)

  - final : 상수화, 고정된 값을 사용할 때 선언
    - 클래스 상속 금지
    - 메소드 오버라이딩 금지


-----------

Java 플랫폼의 이해

- 자바 컴파일러(JAVA Compiler, javac)
- 자바 가상 머신(JVM)
- Runtime Data Area
  - Method Area : 프로그램의 초기 작동을 준비하는데에 필요한 데이터
    - ex ) 클래스 정보, static 변수, 상수 등 / 프로그램 종료 시 까지 생존
  - Heap Area : 프로그램이 실행되는 도중에 생겼다가 사라지는 데이터들을 보관
    - ex ) 인스턴스 정보 / 공간이 꽉 차면 Garbage Collector에 의해 청소됨
  - Stack Area : 프로그램이 실행되는 도중에 생겼다가 사라지는 데이터들을 보관
    - ex ) 메소드 내에서 사용하는 정보(매개 변수, 지역 변수, 리턴 값 등)
    * 자료 구조 방식(Queue)
     - Heap : FIFO(First In First Out)
     - Stack : LIFO(Last In First Out)
  - PC Register : 데이터 공유 시 사용
  - Native Method Stack : 
- 자바 라이브러리(Java Library)

-----------

IT 도서 추천

- 컴퓨터 구조론(Computer Architecture)
- 자료 구조(Data Analysis)
- 운영 체제(Operating System)
- Networking
- 소프트웨어공학(Software Enginering)

-----------

추천 영상

CPU 작동 원리

https://youtu.be/Fg00LN30Ezg?si=3Nsuk9WBi1H6_pue

자료 구조 관련

https://youtu.be/whVUYv0Leg0?si=W7T_LmjFsibqCtC3
https://youtu.be/Vi0hauJemxA?si=kDALSU75pYM91Urb
https://youtu.be/7BDzle2n47c?si=dnGJPoDarA9uivIg
