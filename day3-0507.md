Java 네이밍 규칙

- class : 대문자로 시작 권장
- method : 소문자로 시작 권장
- java 파일 : 파일 내에 있는 public 클래스명과 동일
  - 하나의 자바 파일에는 하나의 public 클래스만 존재
  - public 클래스가 없으면 여러 클래스 중 하나와 동일한 이름을 파일명으로 설정

-------------------------------

소프트웨어 공학 - 효율적인 프로그램 개발 방법

1. 프로그램 설계 관점(디자인 패턴)
  1) 생성 패턴 : 객체의 생성 방식을 결정하는 패턴
     - 싱글톤(Singleton) 패턴 : 클래스의 인스턴스를 하나만 생성
     - 팩토리 메소드(Factory Method) 패턴 : 인터페이스를 생성 
       - 구현 시 인터페이스를 상속해서 실제 구현 가능한 메소드를 생성
     - 추상 팩토리(Abstract Factory) 방식 : 연관된 객체들을 모아서 제품군을 생성
     - 빌더(Builder) : 복잡한 객체의 생성 과정을 단계별로 분리하여, 동일한 생성 절차에서 서로 다른 표현을 만들 수 있게 함.

  2) 구조 패턴
    - 어댑터(Adaptor)
    - 데코레이터(Decorator)
    - 프록시(Proxy)
    - 퍼사드(Facade)
  
  3) 행위 패턴
    - 옵저버(Observer) : 이벤트 리스너, 발행-구독(ex: 카프카) ...
    - 전략(Strategy)
    - 템플릿 메소드(Template Method) : 부모가 설계하고 실제 구현은 자식이 하도록 하는 패턴
    - 상태(State)

 ref. https://ittrue.tistory.com/550 (디자인 패턴 관련 설명 블로그)

2. 프로그램 작성 시 필요한 협업 관계 관리(방법론 : Water-fall, Agile ...)

-------------------------------

생성자(Constructor)
- 인스턴스 생성
- 클래스 내의 인스턴스 변수를 초기화
- 메소드 초기 실행
- 인스턴스 생성 방법 : new 연산자 + 클래스명(인스턴스 변수를 매개변수로 사용)


-------------------------------

여러가지 클래스

- 내부 클래스(Inner Class)
- 열거형 클래스(Enum Class)

-------------------------------

예외 처리(Exception)
 -  IDE에서 컴파일 이전에 런타임 오류가 발생될 수 있는 상황을 인식해서 강제로 예외 처리하여 에러를 방지

에러(Error)와 예외 처리(Exception)의 차이
 - 예외처리는 시스템 에러를 방지하기 위한 예방 절차

예외 처리 방법

1. try-catch(-finally) 예외 처리
2. 상위 클래스 / 커스텀 예외 클래스로 throw 처리

-------------------------------

상속(Inheritance)
 - 클래스 상속
  - 지시어 : extends

 - 추상 클래스 상속
  - 지시어 : extends

 - 인터페이스 상속(다중 상속/구현체(클래스) 생성)
  - 지시어 : implements

*어노테이션(Annotation) : 컴파일러에게 상황을 지시/설명 (ex : @Override @Autowired)

-------------------------------

다형성(Polymorphism)
- 하나의 객체가 여러 가지 타입을 가질 수 있는 능력
- 오버로딩(Overloading), 오버라이딩(Overriding)
   - 오버로딩(Overloading)
     - 같은 이름의 메서드를 매개변수(개수, 타입)만 다르게 해서 여러 개 만드는 것
     - 컴파일 시점에 결정되므로 정적 다형성이라고 한다
       * 업캐스팅(Upcasting)
         - 자식 클래스의 객체가 부모 클래스 타입으로 형변환 되는 것
         - 개별적인 자식들을 공통된 부모의 이름으로 묶어서 한번에 관리하기 위해 사용
   - 오버라이딩(Overriding)
     - 상속받은 부모의 메서드를 자식이 재정의
     - 실행 시점(Runtime)에 결정되므로 동적 다형성이라고 한다

-------------------------------

Collections Framework
- 동적 배열 상태를 만드는 객체
- 자주 쓰이는 것 : List, Set, Map

- List <T> list = new ArrayList<>();

- Map <K, V> map = new HashMap<>();

- Generic Class

  - Generic
    - 데이터 타입을 파라미터화하여 재사용성과 안전성을 높이는 기법.
    - 클래스나 메서드에서 사용할 내부 데이터 타입(ex : List<T> 에서 T)을 미리 지정하여 컴파일러에게 통지하는 것
    - 컴파일 시 타입 검사(Type Check) 수행 가능
ex )

List<String> memberList = new ArrayList<>(); 생성 시,
 memberList에 String이 아닌 값을 넣으려고 하면 컴파일 에러가 발생 ---> 런타임 에러 방지 가능

- 와일드 카드
  - 상한 경계 와일드 카드 : 꺼내서 읽는 용도
    - <? extends 클래스명> : 지정된 클래스의 자식 클래스만 타입으로 받을 수 있도록 제한
  - 하한 경계 와일드 카드 : 데이터 넣는 용도
    - <? super 클래스명> : 지정된 클래스와 그 부모 클래스만 허용

-------------------------------

자료 구조 관련 추천 유튜브 채널

https://www.youtube.com/@engineerkorea

-------------------------------

내일 학습 내용

- 람다 표현식
- Stream API
- JDBC
- 반응형 웹
