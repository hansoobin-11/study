# Next.js

### 사용 목적
- CSR의 단점(SEO : 검색 엔진 최적화)을 극복하기 위한 SSR 및 SSG 구현
- 파일 기반 라우팅 구현
  - React Route 방식 : Route 정보를 일일히 App.js에 등록해야함
  - Next.js는 디렉토리 구성으로 Routing 경로를 설정 -> 서비스 시 URL 경로 설정

### 화면 설계
- / : app 내의 page.tsx
- layout.tsx : 전체 화면 구성을 설정 -> globals.css를 import해서 전체 영역에 영향
- 각각의 페이지에서 다른 스타일 적용하려면 디렉토리명.module.css 파일을 폴더 아래에 생성 후 page.tsx에서 import 함

```typescript
import styles from './디렉토리명.module.css';
```

- Image에서 priority : true 시 우선 로드 설정(Lazy Loading 비활성화) -> 시스템 부하 유발 가능성있음(배너 광고/메인 히어로 이미지에 사용)

### Next.js 설정
- 개발 모드에서 두 번씩 렌더링 되는 옵션 중단하기 위해 next.config.mjs 에서 reactStrictMode 를 false 로 설정

  #### cf. 'use client' 지시어
    - 파일의 최상단에 표기
    - React Hook(useState, useEffect...), 브라우저 API(window, navigator...), 이벤트 핸들링(onClick, onChange...), 전역 상태 관리 라이브러리(Recoil, Zustand...), Next.js 전용 클라이언트 Hook(useSearchParams, useRouter...) 사용 시 필요한 경우 사용
    - 컴포넌트 단위로 사용
    - 과하게 사용 시 성능 저하 가능성 및 페이지 로딩 속도 저하 가능성 있음

### 배포
- trailing slash 설정 -> www.boardnext.com/list/index.html
- Next.js : next.config.ts, package.json 설정
- Spring Boot : gradlew build -> compile, jar 생성 -> java -jar 아티팩트.jar(build\libs 폴더 내부에 위치/백엔드 서버 실행 명령)
- 웹서버에서 가상 도메인 등록(httpd.vhosts.conf 에서 VirtualHost *:80 설정) 후 웹서버 재시작(Apache24\bin\httpd -k restart)

##### cf. 아티팩트 jar 파일 컴파일과 웹서버를 함께 실행
### Port Forward
ex ) 외부 노출 포트와 IP 주소를 내부로 연결해서 서비스 가능

# JPA(Java Persistent Application) - ORM

### Spring Boot 에서 JPA 사용법
 
 0. 스프링 부트 설치 시 Spring Data JPA 외부 라이브러리 설치
 1. 데이터 저장소 구성

    1-1. com.board.entity 패키지 생성
      - 실제 물리 설계로 만들어진 테이블 구성도를 참고하여 테이블과 1:1 매핑 가능한 클래스 생성
      - @Entity(name = "board")
      - @Table(name = "jpa_board") : DB내의 테이블 명 - 사용 안하면 Entity 이름으로 생성
      - @Id : primary key 직접 할당
      - @Column : DB내의 컬럼명
      - @GeneratedValue(strategy = GenerationType.SEQUENCE, generator = "BOARD_SEQ") : PK 생성 전략(자동 할당)
       - SEQUENCE : 시퀀스가 있는 DBMS에 적용(Oracle, PostgreSQL...)
       - IDENTITY : 기본키 생성을 DB에 일임하는 방식(MySQL, MariaDB ...)
       - TABLE : Id 관리용 테이블(@TableGenerator) 만들어서 사용 - 성능 저하로 실무에서 거의 안씀
       - AUTO : JPA가 DB 방언(Dialect) 에 맞춰 Hibernate가 자동 선택해서 사용(주의 필요)
         - DB 방언(Dialect) : ORM에서 특정 DB에서 사용하는 고유의 SQL 문법
         - ex ) spring.jpa.database-platform=org.hibernate.dialect.OracleDialect
         - 최신 버전의 Spring Boot 에서는 spring.jpa.database=oracle 만 설정해도 Dialect 인식 가능
       - @SequenceGenerator : SEQUENCE 전략 사용 시 세부 사항 정의하는 어노테이션
      - @ManyToOne(fetch = FetchType.LAZY) : N:1 관계 매핑 시 사용하는 어노테이션
        - LAZY : 지연 로딩, 연관된 엔티티를 사용할 때 DB에서 조회/성능 최적화
        - EAGER : 즉시 로딩, 연관된 엔티티를 처음부터 DB 조회(ManyToOne의 기본값)/성능 저하
      - @OnDelete(action = OnDeleteAction.CASCADE) : 참조하는 부모 데이터가 삭제되었을 때의 동작 설정
      - @JoinColumn(name = "email", nullable = false) : 외래키(FK)의 구조와 제약조건 설정

    1-2. com.board.entity.repository 패키지 생성
      - repository 는 반드시 entity 하단에 생성 (repository 객체들을 entity 패키지 내에 만들거나 repository 패키지 생성 후 내부에 생성)
      - Entity와 1:1 매칭 되어야 함
      - JpaRepository<T,ID> : 해당 인터페이스를 상속하여 인터페이스 내부의 CRUD 메소드 사용(SQL문 자동 생성)
      - T : 매핑할 Entity 클래스를 의미(어떤 테이블과 연결할지)
      - ID : 해당 엔티티의 PK 타입(파라미터 타입 정의)
      - JPQL : JPA에서 사용하는 객체 지향 쿼리 언어
      - @Transactional : 메서드 안의 코드들이 전부 성공했을 때에만 DB에 반영, 하나라도 실패하면 RollBack 처리
      - @Modifying : @Query를 통해 작성된 데이터 변경(INSERT, UPDATE, DELETE) JPQL을 정상적으로 실행하기 위한 트리거
    
    1-3. DTO 생성

      - JPA에서 가공 중인 데이터는 DTO에 저장하고 최종 데이터는 Entity에 저장하는 방식을 권장

2. application.properties 설정
      - spring.jpa.database=oracle
      - spring.jpa.hibernate.ddl-auto=update
      - spring.jpa.properties.hibernate.format_sql=false
      - spring.jpa.show-sql=false
   
      - ddl-auto 옵션 종류
        - create : 클래스와 매핑되는 테이블을 자동으로 생성(이미 존재할 경우 삭제하고 재생성)
        - update : 변경된 사항이 있을 때 스키마 변경
        - create-drop : 애플리케이션이 종료될 때 생성했던 테이블 삭제
        - validate : 테이블을 생성하지 않고 엔티티 클래스와 테이블이 정상적으로 매핑되는지 검사
        - none : 위의 4가지 속성을 제외한 나머지 경우

##### cf. Native SQL : 특정 DBMS 가 제공하는 SQL 문법을 직접 사용하는 것
##### cf. JPA Proxy : 성능 최적화 위해 Hibernate가 대리 상속 객체 생성 -> 실제로 데이터가 필요한 시점에 DB 조회(지연 로딩)
##### cf. QueryDSL : 문자열 쿼리 때문에 발생하는 에러를 자바 컴파일러의 힘을 빌려 원천 차단해 주는 프레임워크(타입 안정성, 직관적 쿼리, 동적 쿼리(체이닝) 지원, 재사용 가능)

# OAuth2.0
  - 개방형 표준 인증 및 인가(Authorization) 프로토콜(간편 로그인) / 본질적으로는 인가(권한을 위임)를 위한 프로토콜

### 권한 부여 방식

- Authorization Code Grant(권한 부여 코드 승인 방식) : 토큰을 노출하지 않고 서버-서버 간 토큰 교환(기본, 권장 방식)
- Implicit Grant(암묵적 승인 방식) : 권한 코드 교환 단계 없이 사용자가 로그인하면 브라우저로 토큰 발급(보안 취약, 권장 x)
- Resource Owner Password Credentials Grant(자원 소유자 자격증명 승인 방식) : 클라이언트와 서버가 같은 회사 제품일 경우 사용(보안 취약, 현재는 사용 금지)
- Client Credentials Grant(클라이언트 자격증명 승인 방식) : 사용자 개입 없이 클라이언트가 인증 서버에 접근(Client ID - Client Secret 으로 토큰 발급)
- PKCE(Proof Key for Code Exchange, 픽시) : 실시간 비밀번호 검증을 추가한 보안 강화 기술(주로 Authorization Code Grant와 결합해서 사용 -> 최근 가장 권장하는 방식)

### 작동 방식(Authorization Code Grant 기준)

1. 로그인 요청
2. 권한 요청
3. 인증 및 동의
4. 권한 부여 코드(Authorization Code) 발급
5. Token 교환
6. 데이터 접근

### 장점

- 권한 제어(Scope)
- 편의성

# Linux

### Ubuntu 설치
- DevOps 교재 (36/288) 참고
- 리눅스 사용 시 Hyper-V(윈도우에서 무료 배포, 윈도우즈 프로 운영체제에서 사용 가능) 라는 Hyper Visor 프로그램을 사용
##### cf. Windows Home 사용자는 Oracle Virtual Box 또는 VMWare Workstation Pro 사용
